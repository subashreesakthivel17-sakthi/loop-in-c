#include <stdio.h>

int main()
{
   int  n,unit;
   int totalunit=0;
   scanf("%d",&n);
   for(int i=0;i<n;i++)
   {
      scanf("%d",&unit);
      totalunit+=unit;
   }
       if(n+=unit){
          totalunit;
       }
       printf("Total unit:%d",totalunit);
      
    

   
    return 0;
}